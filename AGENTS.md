Use the design system defined in `src/styles/tokens.css`.
Add new components to `/components` and keep the list alphabetized.
Do not add or modify tests unless the user explicitly requests test work.
Format changed files with the project's Prettier configuration.

<footer class="site-footer full-bleed">
  <div class="site-footer__inner">
    <a href="/components-preview">Components powering this website</a>
    <button type="button" data-cookie-preferences>Cookie preferences</button>
  </div>
</footer>

<style>
  .site-footer {
    color: var(--color-background);
    background: var(--color-footer);
  }

  .site-footer__inner {
    box-sizing: border-box;
    width: min(calc(100% - (2 * var(--page-padding))), var(--width-content));
    min-height: var(--height-footer);
    margin-inline: auto;
    padding-block: var(--space-16);
  }

  .site-footer a,
  .site-footer button {
    color: inherit;
    background: transparent;
    border: 0;
    padding: 0;
    font-family: var(--font-body);
    font-size: var(--text-base);
    font-weight: var(--weight-normal);
    text-decoration: underline;
    cursor: pointer;
  }

  .site-footer a:hover,
  .site-footer button:hover {
    text-decoration: none;
  }

  .site-footer button {
    display: block;
    margin-top: var(--space-3);
  }
</style>

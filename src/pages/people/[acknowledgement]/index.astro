---
import { acknowledgementPages } from "../../../data/acknowledgements";

export function getStaticPaths() {
  return acknowledgementPages.map(({ slug }) => ({
    params: { acknowledgement: slug },
  }));
}

return Astro.redirect(
  `/acknowledgements/${Astro.params.acknowledgement}/`,
  301,
);
---
